[project]
name = "my_llm"
version = "0.1.0"
description = "Пайплан для эксперементов над моделью LLM"
readme = "README.md"
autors = ""

requires-python = ">=3.10"
dependencies = [

]

[dependency-groups]
dev = [

]

[project.optional-dependencies]
cpu = [

]
gpu = [
    
]