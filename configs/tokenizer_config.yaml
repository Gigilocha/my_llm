# Параметры токенизатора
tokenizer:
  algorithm: bpe
  library: tokenizers  # HF tokenizers, для совместимости с AutoTokenizer
  vocab_size: 65536

  train_sample_size: 2_000_000  # кол-во документов для обучения токенизатора, не весь корпус

  special_tokens:
    bos: <|bos|>
    eos: <|eos|>
    pad: <|pad|>
    user_start: <|user_start|>
    user_end: <|user_end|>
    assistant_start: <|assistant_start|>
    assistant_end: <|assistant_end|>
    tool_call_start: <|tool_call_start|>
    tool_call_end: <|tool_call_end|>
    tool_result_start: <|tool_result_start|>
    tool_result_end: <|tool_result_end|>

  split_pattern: "'(?i:[sdmt]|ll|ve|re)|[^\\r\\n\\p{L}\\p{N}]?+\\p{L}+|\\p{N}{1,3}| ?[^\\s\\p{L}\\p{N}]++[\\r\\n]*|\\s*[\\r\\n]|\\s+(?!\\S)|\\s+"

